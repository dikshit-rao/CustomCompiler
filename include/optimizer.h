#ifndef OPTIMIZER_H
#define OPTIMIZER_H

#include <string>
#include <vector>
#include <unordered_map>
using namespace std;

class Optimizer {
public:
    vector<string> optimize(const vector<string>& code);
};

#endif
